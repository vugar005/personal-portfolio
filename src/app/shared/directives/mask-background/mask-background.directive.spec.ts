import { MaskBackgroundDirective } from './mask-background.directive';

describe('MaskBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
